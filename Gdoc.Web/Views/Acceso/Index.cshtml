<section class="content-header">

</section>
<section class="content" ng-controller="acceso_controller as ctrl" ng-cloak>
    <div class="row">
        <div class="col-md-12">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Accesos</h3>
                    <button ng-click="ctrl.activartodosAcceso()" class="btn btn-default btn-sm pull-right"><i class="fa fa-plus" data-placement="top" data-toggle="tooltip" title="Activar todos"></i></button>
                    <button ng-click="ctrl.desactivartodosAcceso()" class="btn btn-default btn-sm pull-right"><i class="fa fa-minus" data-placement="top" data-toggle="tooltip" title="Desactivar todos"></i></button>

                </div>
                <div class="box-body">
                    <div class="row">
                        <div class="col-md-2">
                            @*<label class="pull-right">Nombre Usuario</label>*@
                        </div>
                        <div class="col-md-3">
                            @*<input type="text" class="form-control" ng-model="ctrl.acceso.Usuario.NombreUsuario" placeholder="Usuario">*@
                            @*<md-content>
                                    <form ng-submit="$event.preventDefault()">
                                        <md-autocomplete ng-disabled="ctrl.isDisabled"
                                                         md-no-cache="ctrl.noCache"
                                                         md-selected-item="ctrl.selectedItem"
                                                         md-search-text-change="ctrl.searchTextChange(ctrl.searchText)"
                                                         md-search-text="ctrl.searchText"
                                                         md-selected-item-change="ctrl.selectedItemChange(item)"
                                                         md-items="item in ctrl.querySearch(ctrl.searchText)"
                                                         md-item-text="item.NombreUsuario"
                                                         md-min-length="0"
                                                         placeholder="Usuario"
                                                         md-menu-class="autocomplete-custom-template">
                                            <md-item-template>
                                                <span>{{item.NombreUsuario}}</span>
                                            </md-item-template>
                                        </md-autocomplete>
                                    </form>
                                </md-content>*@
                            <select class="form-control" ng-model="ctrl.usuario" ng-options="tc as tc.NombreUsuario for tc in ctrl.listUsuario" ng-change="ctrl.buscarAccesoSistema(ctrl.usuario)">
                                <option value="">Usuario</option>
                            </select>
                        </div>

                        <div class="col-md-3">
                            <input type="text" class="form-control" ng-model="ctrl.usuario.NombreCompleto" placeholder="Nombre" disabled>

                        </div>
                        <div class="col-md-2">

                        </div>
                        <div class="col-md-2">
                             @*<button ng-click="ctrl.buscarAccesoSistema(ctrl.usuario)" type="submit" name="search" id="search-btn" class="btn btn-default"><i class="fa fa-search" data-placement="top" data-toggle="tooltip" title="Buscar"></i></button>*@
                        </div>
                    </div>
                    <br>
                </div>
                <div class="grid" style="font-size: 12px; height: 400px" ui-grid="ctrl.gridAccesos" ui-grid-pagination ui-grid-auto-resize ui-grid-resize-columns ui-grid-move-columns></div>
                
            </div>
        </div>
    </div>
    <!--modal de editor-->
    @Html.Partial("_CreateAndEdit")

</section>
@section Scripts{
    <script src="~/scripts/appjs/acceso-controller.js"></script>
}

